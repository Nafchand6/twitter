/* import axios from "axios";

const TWEET_REST_API_URL = "http://localhost:8080/tweet";

class TweetService {
 
  getTweets() {
    return axios.get(TWEET_REST_API_URL + "/tweet");
  }

  getTweetById(id) {
    return axios.get(TWEET_REST_API_URL + "/tweet/{id}" + id);
  }

  saveTweet(tweet) {
    return axios.post(TWEET_REST_API_URL, tweet);
  }

  
}

export default new TweetService();  */